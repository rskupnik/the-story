package com.github.rskupnik.thestory.api.command.details

/**
 * A marker interface for object of type [Details]
 *
 * A [Details] object accompanies a command and only serves as a mean of providing
 * detailed parameters necessary to carry out the command.
 *
 * Example: when setting background, we need details on what type of background to set, etc.
 */
interface Details